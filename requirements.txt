# =============================================================================
# ProjectionAI — Production Dependencies
# =============================================================================
# Dependency specs for the *base* installation (unpinned — see uv.lock for the locked tree).
# Install additional AI provider groups as needed:
#   pip install -e ".[gemini,openai]"
# or install everything:
#   pip install -e ".[all]"
# =============================================================================

# -- Core --
PySide6>=6.8
numpy>=1.26
pydantic>=2.5
pydantic-settings>=2.1
platformdirs>=4.1

# -- Computer Vision --
opencv-python>=4.9
scipy>=1.12
scikit-image>=0.23
Pillow>=10.2

# -- 3D --
open3d>=0.18
trimesh>=4.2

# -- Rendering --
ModernGL>=5.10
PyOpenGL>=3.1
glfw>=2.7

# -- Data & Serialization --
orjson>=3.9

# -- Async --
anyio>=4.2

# --- AI Providers (optional) ---
# google-genai>=1.0      # Gemini
# openai>=1.0            # OpenAI
# anthropic>=0.30        # Anthropic
# replicate>=0.25        # Replicate
